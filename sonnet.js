import { lchmod } from "fs";

var longstring = document.getElementById("sonnet").innerHTML; //get all the contents of the id

var word = "orphan"; // word you want to find

var whereword = longstring.indexOf(word); // index of word

var count = longstring.length; // length of entire word

document.body.innerHTML = document.getElementById("sonnet").innerHTML.substr("winter", "yuletide");

document.body.innerHTML = document.body.innerHTML.replace("the","a large");